import React from "react";
import { TailSpin } from "react-loader-spinner";

const Loading = ({}) => {
  return (
    <button disabled={true} className={"auth-button"}>
      <div className="button-loader">
        <TailSpin
          height="20"
          width="20"
          color="white"
          ariaLabel="tail-spin-loading"
          radius="1"
          wrapperStyle={{}}
          wrapperClass=""
          visible={true}
        />
      </div>
    </button>
  );
};

export const Loading2 = ({}) => {
  return (
    <button disabled={true} className={"auth-button"}>
      <div className="button-loader">
        <TailSpin
          height="20"
          width="20"
          color="white"
          ariaLabel="tail-spin-loading"
          radius="1"
          wrapperStyle={{}}
          wrapperClass=""
          visible={true}
        />
      </div>
    </button>
  );
};

export const Loading3 = ({}) => {
  return (
    <button disabled={true} className={"auth-button"}>
      <div className="button-loader">
        <TailSpin
          height="20"
          width="20"
          color="white"
          ariaLabel="tail-spin-loading"
          radius="1"
          wrapperStyle={{}}
          wrapperClass=""
          visible={true}
        />
      </div>
    </button>
  );
};

export const Loading4 = ({}) => {
  return (
    <button disabled={true} className={"auth-button"}>
      <div className="button-loader">
        <TailSpin
          height="20"
          width="20"
          color="white"
          ariaLabel="tail-spin-loading"
          radius="1"
          wrapperStyle={{}}
          wrapperClass=""
          visible={true}
        />
      </div>
    </button>
  );
};

export default Loading;
